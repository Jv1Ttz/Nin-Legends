{
  "buildCommand": "cd server && npx prisma generate --schema prisma/schema.prisma && cd .. && npx esbuild server/src/index.ts --bundle --platform=node --format=esm --outfile=api/_server.mjs --packages=external && cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
